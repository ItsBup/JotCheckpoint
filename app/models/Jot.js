import { generateId } from "../utils/GenerateId.js";

export class Jot {
  constructor(message) {
    this.id = generateId
    this.name = data.name
    this.body = data.body
    this.createdDate = new Date(data.createdDate)

  }
}